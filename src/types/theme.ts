export type Theme = {
  colors: {
    black: string
    white: string

    grayDark: string
    grayMedium: string
    grayLight: string

    purple: string
    pinkDark: string
    pinkLight: string

    shadow: string
    danger: string
    background: string

    hover: {
      purple: string
      danger: string
      grayMedium: string
      grayLight: string
    }
  }
}
